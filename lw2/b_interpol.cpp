#include <iostream>
#include <fstream>
int main()
{
    char* in_file = (char*)malloc(sizeof(char) * 16);
    char* out_file = (char*)malloc(sizeof(char) * 16);
    int new_w, new_h;
    scanf("%s\n", in_file);
    scanf("%s\n", out_file);
    scanf("%d %d", &new_w, &new_h);

    
    return 0;
}